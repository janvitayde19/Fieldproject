#print() using specific data types.
# {:d} is for int, {:f} is for float, {:s} for string

rno = 200
name = 'sakshi chinchkhede'
per = 89.62

print('Rno = {:d} Name = {:s} Per = {:.2f}'.format(rno,name,per))
    
